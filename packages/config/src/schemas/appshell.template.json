{
  "title": "AppshellTemplate",
  "type": "object",
  "properties": {
    "components": {
      "description": "Collection of remote entrypoints",
      "type": "object",
      "additionalProperties": {
        "anyOf": [
          {
            "$ref": "#/definitions/Entrypoint"
          }
        ]
      }
    },
    "module": {
      "description": "Module federation configuration",
      "type": "object"
    },
    "name": {
      "description": "Name of the module",
      "type": "string"
    },
    "overrides": {
      "description": "Overrides to apply",
      "type": "object",
      "properties": {
        "vars": {
          "type": "object"
        }
      }
    },
    "remotes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Names of remotes this package consumes; the application binds each to an address"
    },
    "vars": {
      "description": "Runtime environment configuration",
      "type": "object"
    },
    "visibility": {
      "type": "string",
      "enum": ["public", "private"],
      "description": "Visibility the registry publishes this package as; --visibility overrides it"
    }
  },
  "definitions": {
    "Entrypoint": {
      "title": "Module entrypoint",
      "type": "object",
      "properties": {
        "id": {
          "description": "ID of the remote app",
          "type": "string"
        },
        "metadata": {
          "description": "Metadata associated with the remote app",
          "type": "object",
          "additionalProperties": true
        }
      },
      "required": [],
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
